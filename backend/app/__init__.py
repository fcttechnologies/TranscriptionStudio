"""Backend application package for Transcription Studio.

This file marks the `backend.app` directory as a Python package so
imports like `backend.app.main` resolve correctly when running Uvicorn.
"""
